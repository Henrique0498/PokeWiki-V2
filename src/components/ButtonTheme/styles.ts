import styled from 'styled-components'

export const Container = styled.button`
  /* .light svg {
    animation: activeLightSvg linear;
    animation-duration: ${({ theme }) => theme.animation.duration.medium};
  }

  .light div {
    animation: activeLightDiv linear;
    animation-duration: ${({ theme }) => theme.animation.duration.medium};
  }

  .dark svg {
    animation: activeDarkSvg linear;
    animation-duration: ${({ theme }) => theme.animation.duration.medium};
  }

  .dark div {
    animation: activeDarkDiv linear;
    animation-duration: ${({ theme }) => theme.animation.duration.medium};
  } */

  /* .react-switch-handle {
    display: flex !important;
    justify-content: center !important;
    align-items: center !important;
  } */

  /* @keyframes activeLightSvg {
    from {
      opacity: 0;
      transform: rotate(180deg);
    }

    to {
      opacity: 1;
    }
  }

  @keyframes activeLightDiv {
    from {
      position: absolute;
      margin-left: 0px;
    }

    to {
      position: absolute;
      margin-left: 28px;
    }
  }

  @keyframes activeDarkSvg {
    from {
      opacity: 0;
      transform: rotate(180deg);
      margin-left: 28px;
      z-index: 1;
    }

    to {
      opacity: 1;
      z-index: 1;
      margin-left: 28px;
    }
  }

  @keyframes activeDarkDiv {
    from {
      position: absolute;
      margin-left: 28px;
      z-index: 2;
    }

    to {
      position: absolute;
      z-index: 2;
      margin-left: 0px;
    }
  } */
`
